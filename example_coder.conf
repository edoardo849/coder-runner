#!/bin/bash

DATADIR=--user-data-dir=$HOME/code-server
WORKDIR=$HOME/code
PORT=--port=8443
# Uncomment below for letsencrypt
# CERT=--cert=/etc/letsencrypt/live/<DOMAIN>/fullchain.pem
# CERTKEY=--cert-key=/etc/letsencrypt/live/<DOMAIN>/privkey.pem
CERT=--cert=$HOME/.ssh/coder.crt
CERTKEY=--cert-key=$HOME/.ssh/coder.key

export PASSWORD=your-pass
